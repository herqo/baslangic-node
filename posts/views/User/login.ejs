<% include ../partials/header.ejs %>


    <div class="container">
        <form method="POST" action="/user/login" style="padding-top:50px">
            <div class="form-group">
                <label for="email">Email address</label>
                <input required name="username" type="email" class="form-control" id="email" placeholder="Email">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input required name="password" type="password" class="form-control" id="password" placeholder="Password">
            </div>
            <button type="submit" class="btn btn-success btn-block">
                Giriş Yap

            </button>
            <% message.forEach((messag) => { %>
                <div class='header alert alert-danger alert-dismissible'>
                    <strong>
                        <i class="fa fa-exclamation-circle"></i> ERROR:</strong> <%= messag %>
                    <a href="#" class='close' data-dismiss="alert" aria-label="close">
                        <i class='fa fa-times'></i>
                    </a>
                </div>
                <% }) %>
        </form>
    </div>

    <% include ../partials/footer.ejs %>